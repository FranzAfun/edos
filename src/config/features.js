/**
 * Feature toggles
 * These are runtime flags, not permissions.
 * Later this can come from backend.
 */
export const FEATURES = {
  EXEC_INTELLIGENCE_V2: true,
  FIN_BUDGET_ADVANCED: false,
  CEO_STRATEGIC_INSIGHT: true,
};
